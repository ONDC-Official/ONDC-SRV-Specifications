search:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    intent:
      item:
        category:
          id:
            $ref: "./category.yaml#/category_ids"
      fulfillments: &fulfillments
        type: 
          $ref: "./fulfillments.yaml#/fulfillments/type"
      tags:
        COMMODITY_DETAILS:
          COMMODITY_CODE:
            $ref: "./commodity.yaml#/commodity_name"
on_search:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    catalog:
      fulfillments: &fulfillments
        type: 
          $ref: "./fulfillments.yaml#/fulfillments/type"
      providers:
        items: 
          descriptor: 
            code:
              $ref: "./assaying_parameter.yaml#/assaying_parameters"
init:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    catalog:
      fulfillments: &fulfillments
        type: 
          $ref: "./fulfillments.yaml#/fulfillments/type"
on_init:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    order:
      quote:
        breakup:
          title:
            $ref: "./title_type.yaml#/breakup/title"
      payments:
        $ref: "./payment.yaml#/payment"
confirm:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    order:
      $ref: "./order.yaml#/order"
      fulfillments:
        type: 
          $ref: "./fulfillments.yaml#/fulfillments/type"
      quote:
        breakup:
          $ref: "./title_type.yaml#/breakup"
      payments:
        $ref: "./payment.yaml#/payment"
on_confirm:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    order:
      $ref: "./order.yaml#/order"
      fulfillments:
        type: 
          $ref: "./fulfillments.yaml#/fulfillments/type"
        state:
          $ref: "./fulfillments.yaml#/fulfillments/state"
      quote:
        breakup:
          $ref: "./title_type.yaml#/breakup"
      payments:
        $ref: "./payment.yaml#/payment"
update:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    order:
      payments:
        $ref: "./payment.yaml#/payment"
on_update:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    order:
      $ref: "./order.yaml#/order"
      fulfillments:
        state:
          $ref: "./fulfillments.yaml#/fulfillments/state"
      quote:
        breakup:
          $ref: "./title_type.yaml#/breakup"
      payments:
        $ref: "./payment.yaml#/payment"
status:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
on_status:
  context: &context
    domain: 
      $ref: "./context.yaml#/domain"
  message:
    order:
      $ref: "./order.yaml#/order"
      fulfillments:
        state:
          $ref: "./fulfillments.yaml#/fulfillments/state"
      quote:
        breakup:
          $ref: "./title_type.yaml#/breakup"
      payments:
        $ref: "./payment.yaml#/payment"
